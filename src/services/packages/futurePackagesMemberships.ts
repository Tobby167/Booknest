// Future upgrade point:
// Add packages, memberships, multi-session balances, and redemption rules here.
// The MVP only supports single appointments.

export function packagesMembershipsPlaceholder() {
  return "Packages and memberships are planned for a later BookNest version.";
}
