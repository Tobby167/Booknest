// Future upgrade point:
// Add Google Calendar OAuth and event sync here when automated calendar sync
// becomes part of BookNest. The MVP uses the internal calendar dashboard.

export async function syncGoogleCalendarLater() {
  throw new Error("Google Calendar is not configured in the MVP.");
}
